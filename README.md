# Displaying-Dynamic-Content-JOIN-READ-
SQL commands to create the posts table and insert sample blog data, linking posts to users via author_id. Fetches data from the posts and users tables using a SQL JOIN to display dynamic content (a list of blog posts with author details).
### 4. Displaying Dynamic Content (JOIN READ)

| File | Description |
| :--- | :--- |
| **Database Update** | (Run commands from the assignment description to create the `posts` table). |
| `blog.php` | Fetches and displays blog post content. Demonstrates using a **SQL JOIN** to retrieve post titles, content, and the author's name from two separate tables (`posts` and `users`). |
